# 프론트엔드 서버 포트
PORT=3000

# 백엔드 API URL (JavaScript 파일에서 사용)
# API_BASE_URL은 origin_source/static/js/common/api.js 에서 설정됨

# API Gateway URL (이미지 업로드용)
# - 개발 환경: 주석 처리 또는 빈 값 (Multipart fallback)
# - 프로덕션: API Gateway Invoke URL (백그라운드에서 Lambda → S3)
# - 프론트엔드는 API Gateway를 호출, Lambda는 서버리스 백엔드
# LAMBDA_API_URL=https://{api-id}.execute-api.ap-northeast-2.amazonaws.com
